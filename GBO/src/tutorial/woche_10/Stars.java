package tutorial.woche_10;

import javafx.application.Application;
import javafx.stage.Stage;

public class Stars extends Application
{

    @Override
    public void start(Stage primaryStage) throws Exception
    {
        // TODO Auto-generated method stub

        /*
         * private void dragged(MouseEvent e) { Star star = (Star)e.getSource();
         * double deltaX = e.getX() - clickedX; double deltaY = e.getY() -
         * clickedY; star.xProperty.set(star.xProperty.get() + deltaX);
         * star.yProperty.set(star.yProperty.get() + deltaY); clickedX =
         * e.getX(); clickedY = e.getY(); }
         * 
         * 
         */

    }

    public static void main(String[] args)
    {
        launch(args);
    }

}
